---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: servers.bare-metal-controller.bare-metal.io
spec:
  group: bare-metal-controller.bare-metal.io
  names:
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the servers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server.
            properties:
              control:
                properties:
                  ipmi:
                    properties:
                      address:
                        type: string
                      password:
                        type: string
                      username:
                        type: string
                    required:
                    - address
                    type: object
                  wol:
                    properties:
                      address:
                        type: string
                      broadcastAddress:
                        type: string
                      macAddress:
                        type: string
                      port:
                        default: 9
                        type: integer
                      sshSecretRef:
                        description: SecretReference points to a Kubernetes Secret
                        properties:
                          name:
                            description: Name of the Secret
                            type: string
                          namespace:
                            description: |-
                              Namespace of the Secret (defaults to Server's namespace, but since
                              Server is cluster-scoped, this should be required)
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      user:
                        type: string
                    required:
                    - address
                    - macAddress
                    type: object
                type: object
              powerState:
                enum:
                - "on"
                - "off"
                type: string
              type:
                enum:
                - wol
                - ipmi
                type: string
            required:
            - powerState
            type: object
          status:
            description: ServerStatus defines the observed state of Server.
            properties:
              failingSince:
                format: date-time
                type: string
              failureCount:
                type: integer
              message:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
